import React from 'react'

const Pre = (props) => {
  return (
    <div>
       return 
       <div id={props.load ? "preloader" : "preloader-none"}>
        </div>;
    </div>
  )
}

export default Pre
